import { StyleSheet } from 'react-native';

const styles = (theme: any) => StyleSheet.create({
  bannerContainer:{
    width: "100%",
    height: 300
  },
  banner: {
    width: "100%",
    height: "100%"
  }


});

export default styles;
 